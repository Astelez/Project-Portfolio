# local.rules — Suricata custom rules (SID range 1000000-1999999 reserved for local use)

# Detect TCP SYN scans (very simple example; tune in production)
alert tcp any any -> $HOME_NET any (msg:"LOCAL TCP SYN scan"; flags:S; threshold:type both, track by_src, count 20, seconds 10; classtype:network-scan; sid:1000001; rev:1;)

# SSH brute force (multiple failed auth from same source)
alert tcp any any -> $HOME_NET 22 (msg:"LOCAL SSH brute force attempt"; flow:to_server,established; detection_filter:track by_src, count 8, seconds 120; classtype:attempted-admin; sid:1000002; rev:1;)

# ARP spoof indicator (requires af-packet/pcap and proper decoder)
# Suricata handles ARP anomalies in other ways; consider Zeek scripts as well.